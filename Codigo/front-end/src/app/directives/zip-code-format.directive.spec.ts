import { ZipCodeFormatDirective } from './zip-code-format.directive';

describe('NomeDaDiretivaDirective', () => {
  it('should create an instance', () => {
    const directive = new ZipCodeFormatDirective();
    expect(directive).toBeTruthy();
  });
});
